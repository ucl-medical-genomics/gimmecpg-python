"""Main module."""


def add(x: int, y: int) -> int:
    """Adds two numbers.

    Args:
       x (int): The first number
       y (int): The second number

    Returns:
       int: The result of the two numbers
    """
    return x + y
